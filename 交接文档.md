# 速卖通在线产品违规风险分析 — 项目交接文档

## 一、项目概览

**路径**: 项目根目录（本仓库）
**用途**: 对店小秘导出的速卖通在线产品 Excel，进行 8 大维度自动风险检测，输出风险评级/原因/补救措施 Excel。

**三种运行方式**:

| 方式 | 命令 | 说明 |
|------|------|------|
| Web 界面 | `python app.py` → `http://localhost:5000` | 手动上传、分步操作 |
| 批量命令行 | `python app.py --batch` | 扫描 `input/` 下所有 Excel，自动输出到 `output/` |
| 分发的 exe | 双击 `dist/启动批量分析.bat` | 无 Python 环境的电脑用，开箱即用 |

---

## 二、文件结构

```
aliexpress_risk_analyzer/
├── app.py                        # Flask 入口，含 --batch 批量模式
├── config.py                     # 全局配置（路径/阈值/违禁词/品牌库/白名单/API）
├── rule_index.py                 # Clippings/*.md 规则解析器（单例）
├── risk_evaluator.py             # 风险聚合器 + 高/中风险分列输出
├── output_writer.py              # Excel 输出（5列：风险评级 + 高/中原因 + 高/中补救）
├── aliexpress_risk_analyzer.spec # PyInstaller 打包配置
├── 批量分析.bat                  # 开发环境双击启动批处理
├── Clippings/                    # 26个速卖通官方规则 .md 文件
├── data/
│   ├── category_mapping.xlsx     # 类目ID→路径映射表 2029条（部分）
│   └── category_tree.xlsx        # 【新增】全类目4级树 5813条（完整层级）
├── detectors/
│   ├── base.py                   # BaseDetector 基类
│   ├── text_prohibited.py        # A. 违禁关键词（正则 + 上下文消歧 + AI验证）
│   ├── text_contact_leak.py      # B. 联系方式泄露
│   ├── html_structure.py         # C. HTML结构
│   ├── brand_check.py            # D. 品牌侵权（千问AI全量扫描）
│   ├── search_cheating.py        # E. 搜索作弊
│   ├── image_analysis.py         # F. 图片分析（千问视觉AI）
│   ├── category_mismatch.py      # G. 类目错放（AI + 全树 + 一级类目限定）★优化过
│   ├── fda_claims.py             # H. FDA非法宣称（正则 + AI）
│   └── __init__.py
├── utils/
│   ├── ai_client.py              # 千问API封装（核心，所有AI调用入口）
│   ├── category_loader.py        # 类目加载器（ID映射 + 全树 + 关键词搜索）★优化过
│   ├── excel_io.py               # Excel读写 + 颜色标记
│   ├── html_parser.py            # BeautifulSoup深度解析
│   ├── text_utils.py             # 联系方式检测/is_structured_data/品牌提取
│   ├── json_utils.py             # 安全JSON解析
│   └── image_fetch.py            # 图片下载缓存
├── templates/index.html          # Web前端
├── static/app.js, style.css      # 前端交互
├── input/                        # 批量模式输入（放待分析Excel）
├── output/                       # 批量模式输出（分析结果Excel）
├── uploads/                      # Web模式上传暂存（自动创建）
├── image_cache/                  # 图片下载缓存（自动创建）
└── dist/                         # PyInstaller 打包产物
    ├── aliexpress_risk_analyzer.exe   # 独立可执行文件 ~35MB
    ├── 启动批量分析.bat               # 分发版双击启动
    ├── input/                         # 分发版输入目录
    └── output/                        # 分发版输出目录
```

---

## 三、8 大检测维度

| 维度 | 文件 | 检测方式 | 风险 |
|------|------|---------|------|
| **A** 违禁关键词 | `text_prohibited.py` | 10大类正则 → 4条上下文消歧 → 千问AI验证 | **高** |
| **B** 联系方式泄露 | `text_contact_leak.py` | 微信/QQ/邮箱/中国11位手机号（`(?<!\d)1[3-9]\d{9}(?!\d)`） | 中 |
| **C** HTML结构 | `html_structure.py` | C1隐藏元素/C2外链/C3表格属性不一致/C4隐藏堆砌 | 高(违禁词)/中 |
| **D** 品牌侵权 | `brand_check.py` | **千问AI全量扫描标题**，不依赖品牌库，SAFE_BRANDS白名单过滤 | **高** |
| **E** 搜索作弊 | `search_cheating.py` | E1堆砌(>3次)/E2超长(>128字符)/E3 SKU价差(>10倍) | 中/高 |
| **F** 图片分析 | `image_analysis.py` | **千问视觉AI** 8项检查，仅读产品图片列；品牌/logo→高，水印→中；**家居类有独立宽松规则**（见4.5） | 混合 |
| **G** 类目错放 | `category_mismatch.py` | **★已重写**：树查找验证卖家L4/L3 → AI相关度打分(0-100) → 低分则搜索替代路径。不再依赖AI选类目，根除跨L1幻觉 | **仅美容健康启用**，中风险 |
| **H** FDA非法宣称 | `fda_claims.py` | 4类正则 + 千问AI二次验证 | **高** |

### 风险等级速查

| 高风险 | 中风险 |
|--------|--------|
| 违禁关键词、标题品牌侵权、图片品牌/logo、美容/医药/健康类目错放、HTML隐藏违禁词、FDA非法宣称、SKU价差异常/超低价/零库存 | 联系方式泄露、标题堆砌、标题超长、图片水印、图片联系方式、其他类目错放、HTML隐藏元素(无违禁词) |

---

## 四、类目错放检测（G）— 已重写 ★

### 4.1 核心算法（三步流水线）

**树查找 → AI 打分 → 替代搜索**。AI 只负责相关度评分，不负责选择类目（根除 LLM 跨 L1 幻觉）。

```
输入: 产品标题+描述 + 卖家已选L4/L3名 + 一级类目(目前仅「美容健康」)
  ↓
Step 1: 树查找 — match_category_tiered(chosen_l4, chosen_l3, l1)
  ├─ L4 名在 L1 子树中存在？ → 获取完整路径
  ├─ L4 不存在，L3 存在？ → 获取 L1>L2>L3 路径
  └─ 都不存在 → 「不在该L1下，请核查」（中风险，结束）
  ↓
Step 2: AI 相关度打分 — score_category_match(title, desc, path) → 0-100
  ├─ ≥ 50 分 → 匹配正确，不标记（结束）
  └─ < 50 分 → 进入 Step 3
  ↓
Step 3: 替代路径搜索 — search_categories(title, l1_filter=l1)
  ├─ 找到更匹配的替代路径 → 标记错放，推荐替代路径
  └─ 找不到 → 「有错放风险，请核查」（中风险，结束）
```

### 4.2 为什么重写

旧方案（AI 选择类目）有根本缺陷：
- **LLM 跨 L1 幻觉**：AI prompt 只展示美容健康子树，但 AI 从训练数据中脑补家具路径（如「酒店卧室家具套装」）
- **is_valid_path 放过**：验证只检查路径是否在全局树中存在，AI 幻觉出的家具路径在全局树中确实存在 → 通过验证
- **补丁无效**：曾尝试 L1 一致性校验 + 低分过滤，都是打补丁不治本
- **效果**：旧方案 31/49 产品被错误推荐家具路径，全部高风险

新方案优势：
- AI 只打分不选类目 → 零跨 L1 幻觉
- 树查找是确定性的 → 卖家 L4/L3 在不在 L1 子树中，一查便知
- 不确定时标记「请核查」而非强行推荐 → 中风险而非高风险

### 4.3 涉及文件

| 文件 | 职责 |
|------|------|
| `data/category_tree.xlsx` | 5813条 4级全类目树 |
| `data/category_mapping.xlsx` | 2029条 类目ID→路径映射 |
| `utils/category_loader.py` | `match_category_tiered(l4, l3, l1)` — 树查找，返回路径或 None |
| `utils/ai_client.py` | `score_category_match(title, desc, path)` — AI 打分 0-100 |
| `detectors/category_mismatch.py` | 三步流水线主逻辑；全树模式（无 L1 hint 时）保留旧 AI 逻辑 |

### 4.4 一级类目列要求

- 列名兼容 `一级类目` 和 `一级目录` 两种写法
- **当前仅「美容健康」启用类目错放检测**，其他 L1 直接跳过
- 如需扩展到其他 L1，修改 `category_mismatch.py` 中的守卫条件：`if l1_hint != "美容健康": return results`

### 4.5 已知局限

- 替代路径搜索用 n-gram 关键词匹配，**英文标题在中文类目树中效果差**，容易进入「请核查」分支
- 如果产品类目 ID 在 `category_mapping.xlsx` 中不存在，`get_category_name()` 返回 "Unknown category" → L4/L3 为空 → 树查找失败 → 标记「请核查」
- 类目路径中混用 `->` 和 ` -> ` 两种分隔符，代码已做归一化处理

---

## 五、千问 AI 详解

### 5.1 配置

| 参数 | 值 |
|------|-----|
| API Key | `sk-your-qwen-api-key`（Token Plan 团队版） |
| Base URL | `https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1` |
| 模型 | `qwen3.6-flash`（全部调用统一使用） |
| 超时/重试 | 30s / 1次重试 |
| 关键优化 | `enable_thinking: false` + requests.Session连接池 + IPv4强制(urllib3 monkey-patch) + 内存缓存 |

### 5.2 AI 调用函数（全部在 `utils/ai_client.py`）

| 函数 | 调用场景 | 返回值 |
|------|---------|--------|
| `_call_qwen(messages, ...)` | 底层 HTTP 调用，带缓存和重试；`data_inspection_failed` 会重试1次但不走base64回退 | `str` 或 `None` |
| `_parse_json_response(text)` | 解析 AI 返回的 JSON（四重兜底：`json.loads` → `ast.literal_eval` → bracket-counting嵌套提取 → 失败） | `dict` 或 `None` |
| `_extract_json_block(text)` | 括号计数提取完整 JSON 块（替代原 `\{[^{}]*\}` 正则，支持嵌套花括号） | `str` 或 `None` |
| `verify_prohibited_words(name, matches)` | 违禁关键词/FDA 二次验证 | `{keyword: {is_violation, ai_reason}}` |
| `discover_brands_in_title(title, desc)` | 品牌全量发现+侵权判断（`max_tokens=1000`，prompt要求reason≤15词） | `[{brand_name, is_infringement, reason}]` |
| `analyze_product_images(urls, name, desc, category)` | 视觉AI分析产品图片；新增 `product_category` 参数用于家居类自动切换宽松prompt | `[{image_index, findings}]` |
| `verify_furniture_brand_risk(name, finding)` | **家居类专属**：对视觉AI的品牌finding做文本AI二次确认，过滤背景logo/认证/标识 | `bool` |
| `_is_furniture_category(path)` | 判断类目路径是否属于家居/家具/卫浴/家装 | `bool` |
| `_check_urls_reachable(urls)` | HEAD预检图片URL是否可达（全部失效则跳过视觉AI，节省调用） | `bool` |

### 5.3 category_loader.py 函数（utils/category_loader.py）

| 函数 | 用途 |
|------|------|
| `load_categories()` | 加载类目ID映射（category_mapping.xlsx），返回 `{id: {path, name}}` |
| `get_category(category_id)` | 按ID查单个类目 |
| `get_category_name(category_id)` | 按ID获取可读名称 `"L1 -> L2 -> L3 -> L4"` |
| **`load_full_tree()`** | 加载全类目树（category_tree.xlsx），返回 `{tree, flat_paths, l1_list, l1_l2_map, ngram_index}` |
| **`format_tree_for_prompt(l1_filter=None)`** | 格式化类目树供AI prompt使用。有`l1_filter`时仅返回该L1完整子树 |
| **`search_categories(query, top_n=5, l1_filter=None)`** | 关键词n-gram搜索类目树，支持L1范围限定 |
| **`find_closest_path(text, top_n=3, l1_filter=None)`** | 自由文本→最匹配的真实类目路径，支持L1范围限定 |
| **`is_valid_path(path)`** | 检查路径是否在全树中存在 |

### 5.4 图片分析流程

```
0. HEAD 预检: 快速检查前3张图片URL是否可达 → 全部失效则直接跳过（节省API调用）
1. URL 模式: 图片 URL 直接发给千问 → 千问服务器下载分析
   ↓ 如果失败 (HTTP 400: "Failed to download multimodal content")
2. Base64 回退: 本地下载图片 → 转 base64 data URI → 发给千问
   （Referer: aliexpress.com, timeout=20s, 失败重试1次）
   ↓ 如果 data_inspection_failed → 直接跳过（内容审核拦截，base64一样失败）
   ↓ 如果全部base64下载失败 → 跳过视觉AI调用
3. 返回空 → 该产品图片分析跳过
```

**base64 下载优化**（`_download_image_as_base64`）：
- 添加 `Referer: https://www.aliexpress.com/` 头
- timeout 从 15s 延长到 20s
- 失败后 sleep 1s 重试 1 次

### 5.5 已知 AI 行为特征

- **qwen3.6-flash 偶尔返回 Python 字典**（单引号 `{'brands': []}` 而非 JSON `{"brands": []}`）→ 已用 `ast.literal_eval` 兜底
- **模型偶尔不按 prompt 格式返回**（比如品牌 prompt 要求 `brands` 字段，但模型返回了 `results`）→ 导致 `AI brand discovery failed`，个别产品品牌漏检
- **JSON 嵌套对象解析失败已修复**：原来正则 `\{[^{}]*\}` 只匹配单层花括号 → 已改为 `_extract_json_block()` 括号计数算法，支持字符串内的花括号和转义符
- **品牌 JSON 截断问题已修复**：`discover_brands_in_title` 的 `max_tokens` 从 500 提升到 1000，prompt 要求 reason ≤15 词
- **速卖通图片 URL 有时效性**：已添加 HEAD 预检，全部失效则直接跳过；base64 下载添加 Referer 头和重试
- **`data_inspection_failed` 安全审核拦截**：已区分处理——重试1次（防网络波动），但不触发 base64 回退（内容问题换传输方式无效）
- **qwen3.6-flash 对系统 prompt 的遵守度有限**：用户消息中的放宽指令经常被忽略 → 家居类使用独立系统 prompt + 代码层二次确认

### 5.6 家居/家具类图片品牌检测（重要）

家居类产品（家具、卫浴、家装、厨房、灯具、花园等）的图片品牌检测采用**三层防护**，避免误报：

**第一层 — 类目识别**：
- 通过 `产品类型` 列获取类目 ID → `get_category_name()` 解析为文字路径
- `_is_furniture_category()` 匹配 ~40 个中英文关键词（家具、卫浴、厨、灯、bathroom、furniture 等）

**第二层 — 独立系统 prompt**：
- 家居类使用 `VISION_PROMPT_FURNITURE`（而非通用的 `VISION_PROMPT`）
- 规则 #1 改为：**只有品牌 logo 印刷/雕刻/压印在产品主体上才算侵权**
- 明确排除：认证标记（CE/ISO/WRAS等）、工厂标识、背景建筑logo、资质证书、保险单、中英文招牌、公司介绍、无产品出镜的图片

**第三层 — 文本 AI 二次确认**（`verify_furniture_brand_risk`）：
- 视觉 AI 返回 brand/logo 类 finding 后，发一次轻量文本 AI 确认
- 判断是真侵权（logo印在产品上）还是假阳性（背景/认证/标识）
- 假阳性直接过滤，日志输出 `[Furniture Verify] False alarm filtered: ...`
- 确认失败的保守处理：保留 finding（宁可多报不漏报）

**家居类目关键词**（`_FURNITURE_CATEGORY_KW`）：
```
furniture, home & garden, home improvement, bathroom, kitchen, lighting, garden,
家居, 家具, 家装, 卫浴, 厨, 灯, 花园, 家纺,
书柜, 沙发, 椅, 桌, 柜, 床, 垫, 浴, 马桶, 淋浴,
花洒, 地毯, 墙纸, 地板, 衣柜, 水槽, 浴缸, 便器...
```

**注意**：非家居类产品不受任何影响，品牌检测保持原有严格标准。

---

## 六、路径配置（`config.py`）

所有路径已改为相对于项目根目录，兼容 PyInstaller 打包：

```python
# 开发环境：基于 __file__ 所在目录
# PyInstaller：__file__ 指向临时解压目录 (_MEIPASS)
_BASE_DIR  → 只读资源（Clippings/、data/、templates/、static/）
_APP_DIR   → 可写目录（image_cache/、uploads/、input/、output/）
```

| 路径 | 用途 | 打包后行为 |
|------|------|-----------|
| `Clippings/` | 26 个规则 .md | 打包进 exe（只读） |
| `data/category_mapping.xlsx` | 类目ID→路径映射（2029条） | 打包进 exe（只读） |
| `data/category_tree.xlsx` | 全类目4级树（5813条）★新增 | 打包进 exe（只读） |
| `image_cache/` | 图片下载缓存 | 运行时在 exe 旁自动创建 |
| `uploads/` | Web 上传暂存 | 运行时自动创建 |
| `input/` | 批量模式输入 | exe 旁手动放置 |
| `output/` | 批量模式输出 | 运行时自动输出 |

---

## 七、Excel 输入/输出格式

### 输入要求

店小秘导出的 Excel，**最后一列须为 `一级类目` 或 `一级目录`**（两种列名均兼容）。目前仅「美容健康」触发类目错放检测，但仍建议所有产品填写该列以便后续扩展。

### 输出格式

5 列，按风险等级分离：

| 列名 | 内容 |
|------|------|
| 风险评级 | `高风险有其他违规风险` / `中风险只警告不扣分` / `低风险无违规风险` |
| 高风险原因 | 仅高风险发现，带规则溯源 `[规则文件名] 条款`，类目错放含 `（一级类目: xxx）` |
| 高风险补救措施 | 仅高风险发现的补救建议，类目错放含 `推荐路径: L1 > L2 > L3 > L4` |
| 中风险原因 | 仅中风险发现 |
| 中风险补救措施 | 仅中风险发现 |

---

## 八、PyInstaller 打包

### 构建命令

```bash
python -m PyInstaller --noconfirm aliexpress_risk_analyzer.spec
```

### 产物

```
dist/
├── aliexpress_risk_analyzer.exe   (~35MB，单文件)
├── 启动批量分析.bat
├── input/
└── output/
```

### 修改后重建流程

1. 改代码
2. `rm -rf dist/aliexpress_risk_analyzer.exe`
3. `python -m PyInstaller --noconfirm aliexpress_risk_analyzer.spec`
4. 验证：`cp` 一个小 Excel 到 `dist/input/`，运行 `dist/aliexpress_risk_analyzer.exe --batch`

### spec 文件关键配置

- `datas`: `templates/`, `static/`, `data/`, `Clippings/` 打包进 exe
- `hiddenimports`: 8 个 detector + 7 个 util 模块 + `openpyxl`, `bs4`, `lxml`, `urllib3`
- `console=True`: 保留控制台窗口（能看到进度和错误）

---

## 九、配置速查

### `config.py` 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `QWEN_API_KEY` | `sk-your-qwen-api-key` | 更换 key 改这里 |
| `QWEN_API_BASE_URL` | token-plan 北京 endpoint | |
| `QWEN_MODEL` | `qwen3.6-flash` | |
| `AI_REQUEST_TIMEOUT` | 30s | 网络慢可调大 |
| `AI_MAX_RETRIES` | 1 | |
| `SAFE_BRANDS` | 70 个 | 合作品牌白名单（**全小写**） |

**SAFE_BRANDS 白名单更新须知**：

白名单在 **3 个位置** 生效，增删改品牌时必须 **全部同步**：

| # | 文件 | 位置 | 作用 |
|---|------|------|------|
| 1 | `config.py` | `SAFE_BRANDS` 列表 | **代码层过滤**（唯一入口），3 个检测路径都从这里读取 |
| 2 | `utils/ai_client.py` | `VISION_PROMPT` 末尾 | AI 通用图片 prompt 中的自然语言白名单提示 |
| 3 | `utils/ai_client.py` | `VISION_PROMPT_FURNITURE` 末尾 | AI 家居图片 prompt 中的自然语言白名单提示 |

白名单在以下 **3 个检测路径** 中生效（代码层，均从 `config.py` 导入）：

| 检测路径 | 文件 | 过滤方式 |
|---------|------|---------|
| 标题品牌检测 | `detectors/brand_check.py` `_check_title_brands()` | AI 返回的 `brand_name` 精确匹配（大小写不敏感） |
| 图片品牌检测 | `detectors/image_analysis.py` `detect()` | 视觉 AI finding 的 `reason` 字段包含匹配 |
| 图片→品牌桥接 | `detectors/brand_check.py` `_check_image_brands()` | 同上，`reason` 字段包含匹配 |

> **注意**：图片检测路径中，代码层通过 `reason` 字段做字符串包含匹配而非精确匹配（视觉 AI 不返回结构化的 `brand_name`）。如果 AI 的 `reason` 未提及品牌名（如只写 "brand logo visible on product"），代码层无法过滤。此时依赖 prompt 层的自然语言提示作为第一道防线。
| `TITLE_LENGTH_MAX` | 128 | |
| `TITLE_KEYWORD_REPEAT_MAX` | 3 | |
| `SKU_PRICE_RATIO_MAX` | 10 | |
| `PROHIBITED_PATTERNS` | 10 大类 | 违禁品类正则 |
| `FDA_ILLEGAL_PATTERNS` | 4 类 | |
| `KNOWN_MAJOR_BRANDS` | ~150 | 仅供参考，AI 品牌检测不依赖 |
| `TITLE_LENGTH_MAX` | 128 | |

### 品牌检测：测量单位消歧（新增）

**问题**：千问 AI 将标题中的 "3M"（3 米，如 "3M cable"、"3M LED strip"）误判为 3M 品牌。

**修复（双层防护）**：

| 层 | 文件 | 方式 |
|----|------|------|
| AI Prompt | `utils/ai_client.py` → `DISCOVER_BRAND_PROMPT` | 告知 AI：数字+单位的尺寸规格不是品牌名 |
| 代码过滤 | `detectors/brand_check.py` → `_is_measurement_context()` | 品牌名匹配 `\d+M` 且标题含测量关键词时自动跳过 |

测量关键词列表在 `_MEASUREMENT_CONTEXT_KW`，可扩展（当前：cable, wire, strip, cord, LED, HDMI, 线, 绳, 管 等）。
| `CATEGORY_TREE_PATH` | `data/category_tree.xlsx` | ★新增 |

### 上下文消歧（`text_prohibited.py`）

| 关键词 | 安全上下文 | 原因 |
|--------|-----------|------|
| `wine` | + fridge/cooler/cabinet/rack... | 酒柜电器，不是酒 |
| `rum` | + cocktail/syrup/cake/food... | 食品，不是酒 |
| `sex` | + unisex/bisexual... | 合成词 |
| `explosive` | + flammable gas/safety/warning... | 安全说明 |

---

## 十、API 路由（Web 模式）

| 路由 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 主页面 |
| `/api/upload` | POST | 上传 Excel |
| `/api/analyze` | POST | 开始分析 `{detectors: [...]}` |
| `/api/products?risk=&page=&page_size=` | GET | 产品列表（分页+筛选） |
| `/api/product/<index>` | GET | 单产品详情（含高/中风险分列） |
| `/api/export` | GET | 下载结果 Excel |
| `/api/rules` | GET | 规则索引 |

---

## 十一、常见问题排查

| 现象 | 原因 | 解决 |
|------|------|------|
| `[AI Error] Connection error` | 网络不通，无法访问千问 API | 检查防火墙/DNS，确认能访问 `token-plan.cn-beijing.maas.aliyuncs.com` |
| `[AI Error] HTTP 403` | API key 无效或 IP 白名单限制 | 检查 `QWEN_API_KEY`，在阿里云百炼后台添加 IP |
| `[AI Error] HTTP 400: Failed to download multimodal content` | 千问服务器拉不到图片 URL | 自动触发 base64 回退；若回退也失败则图片过期 |
| `[AI Error] HTTP 400: data_inspection_failed` | 图片被千问安全审核拦截 | 重试1次（防网络波动），失败后直接跳过**不触发base64回退** |
| `[Vision] All image URLs unreachable` | 图片 URL 全部过期 | 正常——HEAD 预检发现后跳过，节省了 AI 调用 |
| `[Furniture Verify] False alarm filtered: ...` | 正常日志——家居类的品牌误报被二次确认过滤 | 无需处理 |
| `AI brand discovery failed` | 千问返回格式异常或 JSON 截断 | 已优化（max_tokens=1000 + 嵌套JSON解析），现较少出现 |
| `[AI] JSON parse failed`（视觉分析） | `max_tokens=1500` 不够——多图+多违规时 JSON 响应被截断 | 增大 `analyze_product_images()` 中 `max_tokens`（目前1500，可调到4096） |
| `[AI] JSON parse failed`（文本分析） | 模型返回了无法解析的格式 | 已有 bracket-counting 四重兜底，查看 raw response |
| 3M 等数字+单位被误判为品牌 | AI 未识别测量上下文 | 已有双层防护（prompt + 代码 `_is_measurement_context()`），如仍有误报可扩展 `_MEASUREMENT_CONTEXT_KW` 关键词 |
| 大批量文件（1000+）视觉分析频繁报 JSON parse failed | 同上述 max_tokens 问题 | 调大 `max_tokens` 即可，不影响其他检测器运行（图片维度静默跳过） |
| 进度条显示 GBK 乱码 | Windows CMD 编码问题 | bat 文件已设 `chcp 65001`，不影响功能 |
| 打包后 exe 找不到规则 | `Clippings/` 没有放在项目根目录 | 确认 `Clippings/*.md` 存在，重新打包 |
| 家居类仍有大量品牌误报 | 类目关键词未覆盖该品类 | 在 `ai_client.py` 的 `_FURNITURE_CATEGORY_KW` 中添加关键词 |
| 类目错放大量误报 | 列名不对（`一级目录` vs `一级类目`），或 L1 不是「美容健康」 | 确认列名是 `一级类目` 或 `一级目录`（代码兼容两者）；目前仅美容健康启用，其他 L1 跳过 |
| 类目错放仍推荐家具路径 | 旧代码缓存 | 删 `detectors/__pycache__/` 后重跑；确认 `category_mismatch.py` 包含 `match_category_tiered` + `score_category_match` |

---

## 十二、新对话快速接手

1. **读本文档** → 尤其是第四节（类目错放新算法）和第九节（SAFE_BRANDS 更新须知）
2. **输入数据要求**：最后一列为 `一级类目` 或 `一级目录`（兼容两者）；目前仅「美容健康」触发类目错放检测
3. **启动方式**: `python app.py`（Web）或 `python app.py --batch`（批量）
4. **AI 调不通**: 检查 `config.py` 的 `QWEN_API_KEY`，确认能访问 `token-plan.cn-beijing.maas.aliyuncs.com`
5. **需要重建 exe**: `python -m PyInstaller --noconfirm aliexpress_risk_analyzer.spec`
6. **加/删品牌白名单**: 改 3 个位置 — ① `config.py` → `SAFE_BRANDS`（代码层，全小写）② `utils/ai_client.py` → `VISION_PROMPT` ③ 同文件 `VISION_PROMPT_FURNITURE`。白名单在标题品牌检测 + 图片品牌检测 + 图片→品牌桥接 三个路径均生效
7. **加违禁词**: `config.py` → `PROHIBITED_PATTERNS`
8. **加消歧规则**: `detectors/text_prohibited.py` → `CONTEXT_EXCLUSIONS`
9. **调整风险等级**: 对应 detector 文件中的 `risk_level` 参数
10. **类目错放核心文件**: `detectors/category_mismatch.py`（三步流水线主逻辑）、`utils/category_loader.py` → `match_category_tiered()`（树查找）、`utils/ai_client.py` → `score_category_match()`（AI 打分）
11. **类目错放扩展到其他 L1**: 修改 `category_mismatch.py` 中 `if l1_hint != "美容健康": return results` 守卫条件
12. **类目树数据**: `data/category_tree.xlsx`（5813 条，全树）、`data/category_mapping.xlsx`（ID→路径映射）
13. **规则文件**: `Clippings/*.md`（25 个，已打包进项目）
14. **家居类品牌规则调整**: `ai_client.py` → `VISION_PROMPT_FURNITURE` / `_FURNITURE_CATEGORY_KW` / `verify_furniture_brand_risk()`
15. **AI JSON 解析**: `ai_client.py` → `_extract_json_block()`（嵌套括号计数）/ `_parse_json_response()`（四重兜底）
16. **类目路径分隔符**: 类目映射中混用 `->` 和 ` -> `，代码已在 `category_mismatch.py` 中做归一化处理
17. **品牌数量误判修复**: 如 AI 将 "3M"（3米）判为品牌，检查 `detectors/brand_check.py` → `_is_measurement_context()` 和 `_MEASUREMENT_CONTEXT_KW` 关键词列表
18. **换 API Key 流程**: ① 修改 `config.py` 中的 `QWEN_API_KEY` ② 用 `requests.post` 直接验证 key 可用 ③ `rm -f dist/aliexpress_risk_analyzer.exe && python -m PyInstaller --noconfirm aliexpress_risk_analyzer.spec`
19. **视觉分析 JSON 截断**: 大批量文件时如频繁出现 `[AI] JSON parse failed` + 截断，增大 `analyze_product_images()` 中的 `max_tokens`（当前1500）
20. **家居检测日志已静默**: 原先每个家居产品都打印的 `[Vision] Furniture/home product detected` 已删除，仅保留 `[Furniture Verify] False alarm filtered`（仅在真正过滤误报时出现）
